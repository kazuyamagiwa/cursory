---
description: Python testing conventions for Cursor Cloud Agents
globs:
  - "**/*.py"
  - "tests/**"
  - "scripts/**"
alwaysApply: false
---

# Python testing rules

- Prefer the project's pinned Python via `uv` and `./scripts/run-python.sh`.
- Always run Python and tests via `./scripts/run-python.sh`, never bare `python3` or `pytest`.
- Run tests before committing: `./scripts/run-python.sh --test`
- Never overwrite `pyproject.toml`; edit it in place, then `--sync`.
- Use `pytest` style tests; keep tests focused on real behavior.
